import { SplitDateToDayPipe } from './split-date-to-day.pipe';

describe('SplitDateToDayPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitDateToDayPipe();
    expect(pipe).toBeTruthy();
  });
});
